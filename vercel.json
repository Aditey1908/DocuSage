{
  "installCommand": "npm install --prefix frontend_new",
  "buildCommand": "npm run build --prefix frontend_new",
  "outputDirectory": "frontend_new/.next",
  "framework": "nextjs",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
